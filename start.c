#include <stdio.h>
#include <stdlib.h>
#include "head/robot_header.h"

int main1() {
    /*
    printf("********************start the robot************************\n");
    puts("----init the brain----");
    int brain_init_success = init_head(10000);
    if (brain_init_success != 0) {
        printf("----init brain failed----");
        return -1;
    }

    puts("----init the body----");

    printf("********************robot started************************\n");
     */
    return 0;
}